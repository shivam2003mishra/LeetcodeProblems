class Solution {
    public int linearSearch(int nums[], int target) {
		//Your code goes here
        int index=-1;
        for(int i=0;i<nums.length;i++){
            if(nums[i]==target){
                index=i;
                break;
            }
        }
        return index;
    }
}
